# AGENTS.md

## 项目定位

本项目是模块化开发的嵌入式 Linux 智慧停车场管理系统。

- 当前硬件验证平台：正点原子 i.MX6ULL。
- 当前摄像头：OV5640。
- 后续目标平台：RK3576。
- i.MX6ULL 用于当前模块验证，不代表所有平台相关实现都能直接用于 RK3576。

项目按模块逐步实现。原则上，一个 AI 聊天窗口只负责一个模块。

## 最高优先级：模块隔离

开始修改前，必须从用户请求中确定“当前负责模块”。

- 如果当前模块不明确，先询问用户；在确认前只允许阅读和分析，不允许修改文件。
- 默认只允许修改当前模块的文件和对应测试。
- 不得顺便修复、重构、格式化或删除其他模块的代码。
- 发现其他模块的问题时只报告，不修改。
- 如果任务必须跨模块修改，先停止修改并向用户说明原因、文件范围和接口影响；得到明确同意后才能继续。
- 用户要求实现一个模块，不自动代表允许重构其他模块。

`AGENTS.md` 规则不能替代 Git 隔离。多个聊天窗口并行工作时，应优先使用不同 Git 分支或不同 worktree。

## 当前目录结构

```text
Parking/
├── AGENTS.md
├── CMakeLists.txt
├── PROJECT_STATUS.md
├── include/
│   ├── camera.h
│   └── log.h
├── src/
│   ├── camera/
│   │   └── camera.c
│   ├── log/
│   │   └── log.c
│   └── main.c
├── tests/
│   └── test_camera.c
└── 智慧停车场管理系统项目规划md
```

新增模块时优先遵循：

```text
include/<module>.h
src/<module>/<module>.c
tests/test_<module>.c
```

不要在没有必要时移动已有模块或大规模调整目录结构。

## 当前模块文件边界

### Camera 模块

允许范围：

```text
include/camera.h
src/camera/camera.c
tests/test_camera.c
```

Camera 模块只负责 V4L2 摄像头采集，包括设备打开、格式协商、MMAP 缓冲、视频流控制和帧获取/归还。

`tests/test_camera.c` 可以包含 framebuffer 演示代码，但正式 framebuffer 功能应放入后续独立 display 模块，不得加入 `src/camera/camera.c`。

### Log 模块

允许范围：

```text
include/log.h
src/log/log.c
```

Log 模块只负责日志级别、格式和输出，不实现业务逻辑。

### App/集成层

允许范围：

```text
src/main.c
```

`main.c` 只负责模块初始化、模块连接、主循环和退出清理，不应包含模块内部实现。

### 共享构建文件

```text
CMakeLists.txt
```

它是共享文件。开发新模块时，只允许做注册该模块所需的最小修改，例如增加源文件、库、测试目标或必要链接项。

不得借此修改交叉编译器、C 标准、已有目标语义或删除其他模块目标。若确实需要，先征得用户同意。

### 项目状态与规划

```text
PROJECT_STATUS.md
智慧停车场管理系统项目规划md
```

- `PROJECT_STATUS.md` 用于记录已完成、正在开发、待验证和已知问题。
- 项目规划文档描述总体目标，不应因单个模块任务而随意修改。
- 修改这些文档必须是用户明确要求，或属于当前任务明确要求的交付内容。

## 公共接口保护

`include/` 下供其他模块使用的内容属于公共接口，包括：

- 函数名、参数和返回值。
- 公共结构体和字段语义。
- 枚举、常量和错误码。
- 数据所有权与生命周期。
- 线程安全和阻塞行为约定。

未经用户明确同意，不得删除、重命名或破坏兼容性。

需要修改公共接口时，必须先：

1. 查找所有调用方。
2. 说明修改原因。
3. 说明兼容性影响。
4. 列出需要同步修改的文件。
5. 获得用户同意。

添加新接口时优先保持已有接口可用。

## 模块依赖原则

推荐依赖方向：

```text
app / parking_service
        ↓
公共模块接口
        ↓
camera / display / recognition / storage / network / gate
        ↓
platform / driver
```

必须遵守：

- 模块之间通过公共头文件和明确的数据结构通信。
- 底层模块不得反向依赖业务层。
- 不直接访问其他模块的私有状态。
- 不包含其他模块的私有头文件。
- 不使用跨模块可变全局变量传递状态。
- 不在驱动模块中加入停车业务、数据库、MQTT 或 UI 逻辑。
- 不复制其他模块实现来绕过正式接口。

## Camera API 契约

Camera 的正常生命周期为：

```c
camera_open();
camera_start();

while (running) {
    camera_get_frame();
    /* 使用当前帧 */
    camera_release_frame();
}

camera_stop();
camera_close();
```

`CameraFrame.data` 指向 V4L2 MMAP 缓冲区，是借用内存：

- 调用方不得 `free(frame.data)`。
- 每次成功获取帧后必须归还一次。
- 归还后不得继续访问该地址。
- 需要跨线程或长期持有时，必须在归还前复制数据。
- 不得将一个 Camera 实例的帧归还给另一个实例。

未经用户明确要求，不得改变这一所有权模型。

## 当前硬件约定

i.MX6ULL 当前验证环境：

```text
OV5640 节点：/dev/video1
Framebuffer：/dev/fb0
```

当前驱动枚举支持 `RGBP`、`JPEG` 和 `YUYV`。已知约束：

- RGB565 适合当前 LCD 实时显示验证。
- YUYV 是后续软件转换、识别和 JPEG 编码的可靠输入。
- 驱动枚举 JPEG 不代表直接捕获的数据一定是完整、可打开的 JPEG 文件。
- 不得通过修改扩展名把 RGB565 或 YUYV 原始数据当成 JPEG。
- RK3576 的设备节点、媒体拓扑和采集后端可能不同，不得把 i.MX6ULL 路径硬编码进业务层。

## 编码规则

- 使用 C99。
- 对外接口名称使用清晰、稳定的英文。
- 成功返回 `0`，失败返回非零值；沿用现有模块约定。
- 使用 `LOG_DEBUG`、`LOG_INFO`、`LOG_WARN` 和 `LOG_ERROR`。
- 系统调用失败日志应包含 `strerror(errno)`。
- 所有关键系统调用和 `ioctl()` 必须检查返回值。
- 正确处理 `EINTR`、非阻塞 I/O、超时和部分初始化失败。
- 每条失败路径都必须释放此前成功申请的资源。
- 避免新增全局可变状态。
- 不做与当前任务无关的大范围格式化。

## 构建与验证

工程使用 CMake。常用命令：

```bash
cmake -S . -B build
cmake --build build --clean-first -j2
```

代码应尽量通过：

```text
-Wall -Wextra -Werror
```

修改完成后必须：

1. 编译当前模块对应目标。
2. 编译整个工程，检查是否破坏其他模块。
3. 执行 `git diff --check`。
4. 检查最终变更列表，确认没有无关文件。
5. 报告未能执行的板端测试。

当前构建产物可能是 ARM 可执行文件，不能在 x86 主机直接运行。交叉编译成功只表示编译验证成功，不得声称硬件运行验证成功。

## 工作区与 Git 安全

工作区可能包含用户或其他聊天窗口尚未提交的修改。

- 修改前执行只读状态检查。
- 不覆盖、不回退、不删除来源不明的改动。
- 不使用 `git reset --hard`、`git checkout --` 或类似破坏性命令。
- 不擅自删除文件、清理构建目录或提交代码。
- 如果当前模块文件已被其他窗口修改，先说明冲突风险再继续。
- 多窗口并行开发优先使用独立分支或 worktree。

## 每个模块窗口的工作流程

每个新聊天窗口应执行：

1. 阅读本文件。
2. 明确当前模块和允许修改的文件。
3. 阅读该模块公共头文件、实现、测试和必要调用方。
4. 只制定当前模块范围内的修改计划。
5. 实现并进行模块级验证。
6. 进行全工程编译验证。
7. 汇报修改文件、接口影响、测试结果和板端待验证项。

如果用户在窗口开头只说“开发某模块”，允许创建该模块自己的头文件、实现和测试，并对 `CMakeLists.txt` 做最小注册；不允许修改其他业务模块。

## 完成报告要求

最终回复必须明确说明：

- 修改了哪些文件。
- 是否修改公共接口。
- 是否跨模块修改以及原因。
- 执行了哪些构建和测试。
- 哪些结果仅为编译验证。
- 哪些内容仍需在开发板验证。
- 是否发现但未修改其他模块的问题。

不要把“编译通过”描述为“功能已经在硬件上验证”。
